#WAP to Sum of series: 1¹ + 2² + 3³ + … + nⁿ
def power_sum(n):
    total = 0
    for i in range(1, n + 1):
        total += i ** i
    return total

n = int(input("Enter n: "))
print("Power sum:", power_sum(n))
